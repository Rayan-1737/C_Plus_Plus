//It a special method that is automatically called when an object of a class is created.

// To create a constructor, use the same name as the class, followed by parentheses ():


//  Automatic Call: It runs automatically when an object is created, no need to call it explicitly.

//  Same Name: It has the same name as the class.

//  No Return Type: Unlike regular functions, it doesn't have a return type (not even void).

//  Initialization Only: It is specifically designed to initialize objects.